# purpose: Example inventory for netbox_seed role tests
# maintainer: HybridOps.Tech

[seed_controller]
localhost ansible_connection=local

[seed_controller:vars]
netbox_seed_smoke_only=true
# For real runs, set netbox_seed_repo_root and keep NETBOX_API_URL / NETBOX_API_TOKEN
# in netbox_seed_secrets_env_path on the target host.
# netbox_seed_repo_root=/srv/hybridops
